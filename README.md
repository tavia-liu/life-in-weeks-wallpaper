# Life in Weeks Wallpaper

A lightweight static wallpaper generator. It uses one HTML file with inline CSS and JavaScript, renders to canvas, and exports PNG wallpapers for common laptop sizes or custom dimensions.

## Run locally

Open `index.html` in a browser.

## Publish with GitHub Pages

Serve the repository from the `main` branch and `/` root.
